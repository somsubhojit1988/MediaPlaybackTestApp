<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_playback"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.subhojitsom.playbacktestapp.PlaybackActivity">

     <LinearLayout
        android:id="@+id/url_entry_panel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
         <EditText
             android:layout_alignParentLeft="true"
             android:layout_weight="1"
             android:id="@+id/url_entry"
             android:hint="Enter url here"
             android:layout_height="wrap_content"
             android:layout_width="fill_parent">
         </EditText>
         <ImageButton
             android:id="@+id/play_url_button"
             android:layout_marginTop="10dp"
             android:layout_width="40dp"
             android:layout_height="match_parent"
             android:background="@drawable/play_url"
             ></ImageButton>
     </LinearLayout>

    <SurfaceView
        android:id="@+id/playback_surface"
        android:layout_marginTop="15dp"
        android:layout_below="@id/url_entry_panel"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"/>

   <!-- <ProgressBar
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true"
        android:visibility="gone"
        android:id="@+id/progressBar" />-->
</RelativeLayout>
